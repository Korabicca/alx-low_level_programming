#include<stdio.h>
int main(void)/**
	       * main - Entry point
	       *
	       * Return: Always 0 (Success)
	       */
/**Entry point
*
* Return: Always 0 (Success)
*/  
{
int i;
for(i = 122 ; i > 96 ; i--)
{
putchar(i);
}
putchar('\n');
return (0);
}
